#
# Deploy workflow for Database
#

name: Adminer - Deploy

defaults:
  run:
    working-directory: .
    
on:
  release:
    types: [published]

  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  staging:
    if: github.event.inputs.environment == 'staging'
    name: Deploy to Staging
    uses: ./.github/workflows/infrastructure-adminer-deploy.yml
    with:
      environment: staging
    secrets: inherit

  production:
    if: github.event.inputs.environment == 'production'
    name: Deploy to Production
    uses: ./.github/workflows/infrastructure-adminer-deploy.yml
    with:
      environment: production
    secrets: inherit

